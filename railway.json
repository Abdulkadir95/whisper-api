{
  "build": {
    "env": "python",
    "buildCommand": "pip install -r requirements.txt"
  },
  "start": {
    "cmd": "./start.sh"
  },
  "nixpacks": {
    "phases": {
      "setup": {
        "packages": ["ffmpeg"]
      }
    }
  }
}
